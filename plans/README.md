# Implementation Plans

Generated by the `improve` skill on 2026-06-18 for commit `f18b415`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

These plans correspond to the default direction findings selected after the
audit: VNS search deep links, address-to-Revoke approvals action, token-holder
label filters, and VinuChain feeless/Payback economics on the gas tracker.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Deep-link VNS search results to domain detail pages | P1 | S | none | DONE: implemented and focused verification passed |
| 002 | Add an address-level approvals action to the Revoke dapp | P1 | S | none | DONE: implemented and focused verification passed |
| 003 | Add backend-backed label filters to token holders | P2 | M | backend holder tag filter contract | BLOCKED: live holders API still returns paginated results for impossible `tag_type` filters |
| 004 | Productize feeless and Payback economics on the gas tracker | P2 | M | none | DONE: implemented and focused verification passed |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale: finding fixed independently or approach
abandoned)

## Dependency notes

- 001, 002, and 004 are independent frontend changes and can be executed in any
  order.
- 003 must not start implementation until the backend holder endpoint is
  verified to support global `tag_type` and optional `slug` filtering. A
  client-side filter of the current page is explicitly rejected.
- Rechecked on 2026-06-19: `/api/v2/tokens/{hash}/holders?tag_type=__definitely_not_a_real_tag__`
  still returned 50 items plus pagination, so frontend implementation would be
  misleading without backend support.

## Repo verification baseline

Use the repo's existing Yarn 1 workflow and Node requirement:

- Node: `>=22.14.0`
- Package manager: `yarn@1.22.22`
- Typecheck: `yarn lint:tsc`
- Lint: `yarn lint:eslint`
- Unit tests: `yarn test:vitest`
- Playwright component tests: `yarn test:pw -- <path>`

## Findings considered and rejected

- Client-only token-holder tag filtering: rejected because holder lists are
  paginated and filtering only loaded rows would produce false results.
- New Payback trend or intraday analytics APIs: deferred because plan 004 is a
  frontend-only productization of stats that already exist.
- Refactoring shared VNS routing into a new helper: not necessary for plan 001;
  the existing `EnsEntity` route pattern is simple enough to reuse directly.
